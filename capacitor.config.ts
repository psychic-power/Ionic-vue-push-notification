import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-push-notification',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
