import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.push.notification',
  appName: 'ionic-push-notification',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
